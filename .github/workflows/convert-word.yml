name: Markdown to Word Conversion

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      markdown_file:
        description: 'Path to the markdown file to convert'
        required: false
        default: 'docs/sample.md'
      output_name:
        description: 'Base name for the output file and release tag'
        required: false
        default: 'markdown-to-word'
      title:
        description: 'Document title (defaults to filename if not provided)'
        required: false
        default: ''
      classification:
        description: 'Classification text (e.g., UNCLASSIFIED)'
        required: false
        default: 'UNCLASSIFIED'

jobs:
  markdown-to-word:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Get timestamp
        id: timestamp
        run: echo "TIMESTAMP=$(date +%Y%m%d-%H%M%S)" >> $GITHUB_OUTPUT

      - name: Extract filename from input
        id: filename
        run: |
          if [ -n "${{ inputs.markdown_file }}" ]; then
            BASENAME=$(basename -a "${{ inputs.markdown_file }}" | sed 's/\.md$//')
          else
            BASENAME="combined"
          fi
          echo "BASENAME=$BASENAME" >> $GITHUB_OUTPUT

      - name: Extract title from filename
        id: extract_title
        run: |
          if [ -n "${{ inputs.title }}" ]; then
            TITLE="${{ inputs.title }}"
          else
            # Extract title from markdown filename
            BASENAME=$(basename "${{ inputs.markdown_file }}" .md)
            # Convert to title case (e.g., ra-03 -> RA-03)
            TITLE=$(echo "$BASENAME" | sed 's/-/ /g' | awk '{for(i=1;i<=NF;i++){ $i=toupper(substr($i,1,1)) substr($i,2) }}1')
          fi
          echo "title=$TITLE" >> $GITHUB_OUTPUT

      - name: Install Python dependencies
        run: |
          python3 -m pip install --user -r "${{ github.action_path }}/requirements.txt"
          echo "PYTHONPATH=$HOME/.local/lib/python$(python3 -c 'import sys; print(f"{sys.version_info.major}.{sys.version_info.minor}")')/site-packages:$PYTHONPATH" >> $GITHUB_ENV

      - name: Convert Markdown to Word
        run: |
          python "${{ github.action_path }}/scripts/convert_with_md2docx.py" \
            "${{ inputs.markdown_file != '' && inputs.markdown_file || format('{0}/combined.md', runner.temp) }}" \
            "output/${{ inputs.output_name || 'markdown-to-word' }}-${{ steps.timestamp.outputs.TIMESTAMP }}.docx" \
            "${{ steps.extract_title.outputs.title }}" \
            "${{ inputs.classification }}"

      - name: Upload Word Document
        uses: actions/upload-artifact@v7
        with:
          name: ${{ inputs.output_name || 'markdown-to-word' }}-${{ steps.timestamp.outputs.TIMESTAMP }}
          path: output/${{ inputs.output_name || 'markdown-to-word' }}-${{ steps.timestamp.outputs.TIMESTAMP }}.docx
          archive: false

      - name: Create Release
        uses: actions/create-release@v1
        id: create_release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ inputs.output_name || 'docs' }}-${{ steps.filename.outputs.BASENAME }}-${{ steps.timestamp.outputs.TIMESTAMP }}
          release_name: Documentation release for ${{ inputs.output_name || steps.filename.outputs.BASENAME }}
          body: |
            Hello! This release contains a PDF and DOCX export generated from ${{ inputs.markdown_file || 'combined markdown files' }}.
            
            You can download the files from the release assets below.
          draft: false
          prerelease: false
          commitish: ${{ github.sha }}

      - name: Upload Release Asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: output/${{ inputs.output_name || 'markdown-to-word' }}-${{ steps.timestamp.outputs.TIMESTAMP }}.docx
          asset_name: ${{ inputs.output_name || 'markdown-to-word' }}-${{ steps.timestamp.outputs.TIMESTAMP }}.docx
          asset_content_type: application/vnd.openxmlformats-officedocument.wordprocessingml.document
